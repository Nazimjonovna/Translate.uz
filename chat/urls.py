from django.urls import path
from .views import RoomListView, MessageCreateView, MessageView

urlpatterns=[
    # path('room/', RoomListView.as_view()),
    # path('create_msg/', MessageCreateView.as_view()),
    # path('message/', MessageView.as_view()),
]